import 'package:flutter/material.dart';

class Format{
  String title;
  Color titleColor = Colors.grey[400];
  Color contentColor = Colors.grey[400];
  Color optionPanelColor = Colors.grey[500];

  Color titleTextColor = Colors.black;
  Color contentTextColor = Colors.black;
  Color optionPanelIconColor = Colors.black;
  //font, title size, content size, time created, pin, private, lock
}
